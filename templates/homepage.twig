{% extends "base.twig" %}

{% block bodyclass %}
t-big t-home
{% endblock %}

{% block content %}

	{% include 'includes/header_big.twig' %}

	<div class="js-graph--stats"></div>


	<form method="POST" action="/">
		<fieldset>
			<legend>Set the targets for your training:</legend>
			{% for stat,name in stats %}
				<label>
					<span>{{ name }}</span>
					<input class="js-stat" type="number" name="{{ stat }}" step="4" min="0" max="252" />
				</label>
			{% endfor %}
		</fieldset>

		<fieldset>
			<legend>Game:</legend>
			{% for game,name in games %}
				<label>
					<input type="radio" name="game" value="{{ game }}" {% if game == 0 %} checked {% endif %} >
					<span>{{ name }}</span>
				</label>
			{% endfor %}
		</fieldset>

		<label>
			<input type="checkbox" name="pokerus" value="1">
			<span>Pokerus?</span>
		</label>

		<fieldset>
			<legend>Power items</legend>
			<label>
				<input type="radio" name="item" value="macho">
				<span>Macho Braze</span>
			</label>
			<label>
				<input type="radio" name="item" value="other">
				<span>Other</span>
			</label>
			<label>
				<input type="radio" name="item" value="none" checked>
				<span>None</span>
			</label>
		</fieldset>

		<input type="submit" value="Start training">
		
	</form>

<style>
.js-graph--stats {
	max-width: 400px;
	font-size: .6em;
	font-weight: bold;
	text-transform: uppercase;
	padding: 2em;
	color: #fff;
	text-align: center;
}

.js-graph--stats svg {
	padding: 2em;
}

.js-graph--stats .axis line,
.js-graph--stats .level-group {
	stroke: #f9f9f9;
}

.js-graph--stats .graph--target {
	fill: #fff;
}

.js-graph--stats .graph--start {
	fill: #4da7c9;
	z-index: 100;
	position: relative;
	fill-opacity: 1;
}
.js-graph--stats .legend {
	fill: #fff;
}

.radar-chart .area {
  fill-opacity: 0.9;
}
</style>
<script>
(function(doc) {

	
})(document);
</script>

{% endblock %}